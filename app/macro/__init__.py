"""宏观传导面板。

定位（不可偏离）：**约束器**，不是信号源。
回答三个问题：① 今天宏观环境处在什么历史位置 ② 今天什么变了 ③ 传导链条卡在哪一环。
明确非目标：不产生买卖信号、不给仓位建议、不做涨跌预测。

分层：L0 流动性（有没有水）→ L1 市场内部资金（水流到哪）→ L2 情绪温度 → L3 外部输入。

模块职责：
  store     SQLite 读写（macro.db·独立于 strategy.db）
  registry  指标定义唯一真源 → metric_meta（前端与计算层禁止硬编码指标列表）
  adapters  取数适配器（新增指标只加适配器 + 注册表项）
  compute   派生量：chg/zscore/分位/分层评分/异动判定
  service   面板组装（只读库）
  summary   每日 LLM 摘要（只描述，不预测不建议）
"""
